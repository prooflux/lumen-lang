fn fib_mod(n: Int, m: Int) -> Int {
  var a = 0
  var b = 1
  var i = 2
  while i <= n {
    var next = (a + b) % m
    a = b
    b = next
    i = i + 1
  }
  if n == 0 {
    return a
  } else {
    return b
  }
}

fn main(console: Console) -> Unit {
  console.print_int(fib_mod(90, 1000000007))
}
